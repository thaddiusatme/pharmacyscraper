{
  "api_keys": {
    "google_places": "${GOOGLE_MAPS_API_KEY}",
    "perplexity": "${PERPLEXITY_API_KEY}",
    "apify": "${APIFY_API_TOKEN}"
  },
  "max_results_per_query": 25,
  "output_dir": "data/output/denver",
  "cache_dir": "data/cache/denver",
  "classification_cache_dir": "data/cache/classification/denver",
  "classification_threshold": 0.6,
  "verify_places": true,
  "verification_confidence_threshold": 0.7,
  "max_budget": 100.0,
  "api_cost_limits": {
    "apify": 0.5,
    "google_places": 0.01,
    "perplexity": 0.01
  },
  "locations": [
    {
      "state": "CO",
      "cities": ["Denver"],
      "queries": [
        "independent pharmacy",
        "local pharmacy",
        "compounding pharmacy",
        "neighborhood pharmacy"
      ]
    }
  ]
}
